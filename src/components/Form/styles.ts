import styled from "styled-components";

const StyledForm = styled.form`
  display: flex;
  flex-direction: column;
`;

const Total = styled.p`
  font-weight: 500;
  font-size: 24px;
  line-height: 35px;
  color: #000;
  margin: 45px 0;
`;

export { Total, StyledForm };
